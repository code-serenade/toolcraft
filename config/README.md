## Config Examples

This directory contains example configuration for local development.

- `jwt.example.toml`: example JWT config for `toolcraft-jwt`
- `keys/`: example Ed25519 key files used by `key_dir`

These keys are public demo keys and must not be used in production.
